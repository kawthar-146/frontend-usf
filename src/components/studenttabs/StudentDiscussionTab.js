import React from 'react'

export default function StudentDiscussionTab(props) {
    return (
        <div className={props.toggleState !== 1 ? "unactive_content" : "block"}>
            hell
        </div>
    )
}
